import { userModel } from '../models/userModel';

export const usersController = {
  /* Add users to db */
  addUser: (req, res) => {},
  /* Get users from db */
  getUsers: (req, res) => {},
  /* Get user by id */
  getUserById: (req, res) => {}
};
